import { FileText } from 'lucide-react'
import { PlaceholderPage } from '../components/ui/PlaceholderPage'

export function Forms() {
  return (
    <PlaceholderPage
      icon={FileText}
      title="Forms"
      description="Build custom intake forms and waivers to collect from clients before their booking."
    />
  )
}
