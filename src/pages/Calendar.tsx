import { Calendar as CalendarIcon } from 'lucide-react'
import { PlaceholderPage } from '../components/ui/PlaceholderPage'

export function Calendar() {
  return (
    <PlaceholderPage
      icon={CalendarIcon}
      title="Calendar"
      description="Your full booking calendar — day, week, and month views across every chauffeur and vessel — is coming together here."
    />
  )
}
