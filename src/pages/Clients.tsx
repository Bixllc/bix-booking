import { Users } from 'lucide-react'
import { PlaceholderPage } from '../components/ui/PlaceholderPage'

export function Clients() {
  return (
    <PlaceholderPage
      icon={Users}
      title="Clients"
      description="A searchable directory of every client, their booking history, and notes will live here."
    />
  )
}
